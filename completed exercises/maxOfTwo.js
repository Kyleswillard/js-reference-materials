//Max of 2 numbers
/* write a function that takes 2 numbers and returns the maximum of the 2. call function to ensure it works */

let number = max(1,2)
console.log(number)

function max(num1, num2) {
  return (num1 > num2) ? num1 : num2;
}
